/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost',
          'images.pexels.com',
          'res.cloudinary.com',
          'dashboard-nine-bay-63.vercel.app',
        ],
      },
};

export default nextConfig;
